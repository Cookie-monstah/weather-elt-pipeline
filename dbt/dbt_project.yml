name: 'weather_elt'
version: '1.0.0'

profile: 'weather_elt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  weather_elt:
    staging:
      +materialized: table
    intermediate:
      +materialized: view
    marts:
      +materialized: table
